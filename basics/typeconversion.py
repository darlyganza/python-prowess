# Implicit type conversion
x = 12
y=x+1.6
print (y,type(y))
# Python will automatically assign this to a float type

# Explicit type conversion (I need to explicitly state the data type:
userSalary = int(input("Enter your salary: "))
print(userSalary,type(userSalary))
